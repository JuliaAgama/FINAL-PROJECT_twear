import React from 'react';


export default props => {

    const handleClick = () => {
    };

    return (
        <button
            type="button"
            className="btn btn-primary"
            onClick={handleClick}
        >Add To Cart
        </button>
    )
};
