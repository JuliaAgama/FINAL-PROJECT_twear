import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(theme => (
    {
        mainContainer: {
            gridGap: '1px',
            textAlign: 'center',
            fontWeight: '700',
            fontSize: '16px',
            marginTop: '40px'
        }
    }
));