import React from 'react';


const AddToCart = (props) => {

    const handleClick = () => {
    };

    return (
        <button
            type="button"
            className="btn btn-primary"
            onClick={handleClick}
        >Add To Cart
        </button>
    )
};

export default AddToCart;
